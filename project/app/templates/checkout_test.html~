{% extends "base_nav.html" %}

{% block title %}Checkout{% endblock %}

{% block maincontent %}
<style>
    .cart-page {
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        background: #FDFBE4;
        font-family: 'Gaegu', sans-serif;
        display: flex;
        flex-wrap: wrap;
        padding: 20px;
    }
    .cart-header {
        text-align: center;
        color: black;
        font-size: 64px;
        font-weight: 700;
        margin-top: 20px;
        width: 100%;
    }
    .cart-items, .order-summary, .shipping-info, .payment-info, .promo-code {
        background: rgba(163, 89, 80, 0.20);
        border-radius: 30px;
        padding: 20px;
        margin: 10px;
        flex: 1 1 45%;
    }
    .cart-item {
        display: flex;
        align-items: center;
        margin-bottom: 20px;
    }
    .cart-item img {
        width: 164px;
        height: 242px;
        margin-right: 20px;
    }
    .cart-item-details {
        flex: 1;
    }
    .cart-item-details h3, .cart-item-details p {
        color: black;
    }
    .cart-item-actions p {
        color: black;
        cursor: pointer;
    }
    .summary-box h2 {
        color: white;
        background: #A35950;
        padding: 10px;
        border-radius: 30px 30px 0 0;
        text-align: center;
    }
    .summary-box p {
        color: black;
        font-size: 18px;
        font-weight: 700;
        margin: 5px 0;
    }
    .change-link {
        color: black;
        text-decoration: underline;
        cursor: pointer;
        float: right;
    }
    .checkout-summary {
        background: rgba(163, 89, 80, 0.20);
        border-radius: 30px;
        padding: 20px;
        margin: 10px;
        width: 300px;
        align-self: flex-start;
    }
    .checkout-summary h2 {
        color: white;
        background: #A35950;
        padding: 10px;
        border-radius: 30px 30px 0 0;
        text-align: center;
    }
    .checkout-summary p {
        color: black;
        font-size: 18px;
        font-weight: 700;
        margin: 5px 0;
    }
    .checkout-button {
        width: 100%;
        background: #28a745;
        border-radius: 30px;
        text-align: center;
        padding: 10px;
        color: white;
        font-size: 24px;
        font-weight: 700;
        cursor: pointer;
        display: block;
        text-decoration: none;
        margin-top: 10px;
    }
    .promo-code input[type="text"], .promo-code button {
        width: calc(100% - 40px);
        margin: 5px;
        padding: 10px;
        border: none;
        border-radius: 5px;
        font-size: 18px;
    }
    .promo-code button {
        background: #A35950;
        color: white;
        cursor: pointer;
    }
</style>

<div class="cart-page">
    <h1 class="cart-header">Checkout</h1>
    <div class="shipping-info summary-box">
        <h2>Shipping Information</h2>
        <a href="{% url 'update_shipping_info' %}" class="change-link">Change</a>
        <p>Delivery Address: {% if saved_shipping %}{{ saved_shipping.address }}{% else %}No shipping information saved.{% endif %}</p>
        <a href="#">Add delivery instructions</a>
    </div>
    <div class="payment-info summary-box">
        <h2>Payment Information</h2>
        <a href="{% url 'update_payment_info_nopk' %}" class="change-link">Change</a>
        <p>Paying with: {% if saved_cards %}**** **** **** {{ saved_cards.last.card_number|slice:'-4:' }}{% else %}No payment information saved.{% endif %}</p>
        <a href="#">Use gift card payment</a>
    </div>
    <div class="order-summary summary-box">
        <h2>Order Summary</h2>
        {% for item in cart.cartitem_set.all %}
        <div class="cart-item">
            <img src="{{ item.book.cover_picture.url }}" alt="{{ item.book.title }} cover">
            <div class="cart-item-details">
                <h3>{{ item.book.title }}</h3>
                <p>By: {{ item.book.authors }}</p>
                <form method="post" action="{% url 'update_cart_item' item.id %}">
                    {% csrf_token %}
                    <label for="quantity">Quantity:</label>
                    <input type="number" id="quantity" name="quantity" value="{{ item.quantity }}" min="1">
                    <button type="submit">Update</button>
                </form>
                <p>Price: ${{ item.book.selling_price }}</p>
                <form method="post" action="{% url 'delete_cart_item' item.id %}">
                    {% csrf_token %}
                    <button type="submit">Delete</button>
                </form>
            </div>
        </div>
        {% endfor %}
    </div>
    <div class="promo-code summary-box">
        <h2>Codes</h2>
        <p>If you have promotional codes from our newsletter, please enter them here to save money!</p>
        <form method="post" action="{% url 'apply_promotion' %}">
            {% csrf_token %}
            <input type="text" name="code" placeholder="Enter code here...">
            <button type="submit">Submit Code</button>
        </form>
    </div>
    <div class="checkout-summary">
        <h2>Checkout</h2>
        <p>Items: ${{ total_price }}</p>
        <p>Tax: ${{ tax }}</p>
        <p>Shipping and Handling: ${{ shipping }}</p>
        <p>Order Total: ${{ order_total }}</p>
        {% if user.is_authenticated %}
        <a href="{% url 'checkout_test' %}" class="checkout-button">Confirm and Pay</a>
        {% else %}
        <p>Please <a href="{% url 'login' %}">login</a> to proceed to checkout.</p>
        {% endif %}
    </div>
</div>
{% endblock maincontent %}
